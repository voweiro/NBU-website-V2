/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [], // Add any external domains if needed
    unoptimized: true, // Try this if having optimization issues
  },
};

module.exports = nextConfig;
